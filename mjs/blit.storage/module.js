// Generated by Ignite ignite.com/cli
import { SigningStargateClient } from "@cosmjs/stargate";
import { Registry } from "@cosmjs/proto-signing";
import { msgTypes } from './registry';
import { Api } from "./rest";
import { MsgCreateStorage } from "./types/blit/storage/tx";
import { MsgUpdateStorage } from "./types/blit/storage/tx";
import { MsgDeleteStorage } from "./types/blit/storage/tx";
import { Params as typeParams } from "./types";
import { Storage as typeStorage } from "./types";
export { MsgCreateStorage, MsgUpdateStorage, MsgDeleteStorage };
export const registry = new Registry(msgTypes);
function getStructure(template) {
    const structure = { fields: [] };
    for (let [key, value] of Object.entries(template)) {
        let field = { name: key, type: typeof value };
        structure.fields.push(field);
    }
    return structure;
}
const defaultFee = {
    amount: [],
    gas: "200000",
};
export const txClient = ({ signer, prefix, addr } = { addr: "http://localhost:26657", prefix: "cosmos" }) => {
    return {
        async sendMsgCreateStorage({ value, fee, memo }) {
            if (!signer) {
                throw new Error('TxClient:sendMsgCreateStorage: Unable to sign Tx. Signer is not present.');
            }
            try {
                const { address } = (await signer.getAccounts())[0];
                const signingClient = await SigningStargateClient.connectWithSigner(addr, signer, { registry, prefix });
                let msg = this.msgCreateStorage({ value: MsgCreateStorage.fromPartial(value) });
                return await signingClient.signAndBroadcast(address, [msg], fee ? fee : defaultFee, memo);
            }
            catch (e) {
                throw new Error('TxClient:sendMsgCreateStorage: Could not broadcast Tx: ' + e.message);
            }
        },
        async sendMsgUpdateStorage({ value, fee, memo }) {
            if (!signer) {
                throw new Error('TxClient:sendMsgUpdateStorage: Unable to sign Tx. Signer is not present.');
            }
            try {
                const { address } = (await signer.getAccounts())[0];
                const signingClient = await SigningStargateClient.connectWithSigner(addr, signer, { registry, prefix });
                let msg = this.msgUpdateStorage({ value: MsgUpdateStorage.fromPartial(value) });
                return await signingClient.signAndBroadcast(address, [msg], fee ? fee : defaultFee, memo);
            }
            catch (e) {
                throw new Error('TxClient:sendMsgUpdateStorage: Could not broadcast Tx: ' + e.message);
            }
        },
        async sendMsgDeleteStorage({ value, fee, memo }) {
            if (!signer) {
                throw new Error('TxClient:sendMsgDeleteStorage: Unable to sign Tx. Signer is not present.');
            }
            try {
                const { address } = (await signer.getAccounts())[0];
                const signingClient = await SigningStargateClient.connectWithSigner(addr, signer, { registry, prefix });
                let msg = this.msgDeleteStorage({ value: MsgDeleteStorage.fromPartial(value) });
                return await signingClient.signAndBroadcast(address, [msg], fee ? fee : defaultFee, memo);
            }
            catch (e) {
                throw new Error('TxClient:sendMsgDeleteStorage: Could not broadcast Tx: ' + e.message);
            }
        },
        msgCreateStorage({ value }) {
            try {
                return { typeUrl: "/blit.storage.MsgCreateStorage", value: MsgCreateStorage.fromPartial(value) };
            }
            catch (e) {
                throw new Error('TxClient:MsgCreateStorage: Could not create message: ' + e.message);
            }
        },
        msgUpdateStorage({ value }) {
            try {
                return { typeUrl: "/blit.storage.MsgUpdateStorage", value: MsgUpdateStorage.fromPartial(value) };
            }
            catch (e) {
                throw new Error('TxClient:MsgUpdateStorage: Could not create message: ' + e.message);
            }
        },
        msgDeleteStorage({ value }) {
            try {
                return { typeUrl: "/blit.storage.MsgDeleteStorage", value: MsgDeleteStorage.fromPartial(value) };
            }
            catch (e) {
                throw new Error('TxClient:MsgDeleteStorage: Could not create message: ' + e.message);
            }
        },
    };
};
export const queryClient = ({ addr: addr } = { addr: "http://localhost:1317" }) => {
    return new Api({ baseURL: addr });
};
class SDKModule {
    query;
    tx;
    structure;
    registry = [];
    constructor(client) {
        this.query = queryClient({ addr: client.env.apiURL });
        this.updateTX(client);
        this.structure = {
            Params: getStructure(typeParams.fromPartial({})),
            Storage: getStructure(typeStorage.fromPartial({})),
        };
        client.on('signer-changed', (signer) => {
            this.updateTX(client);
        });
    }
    updateTX(client) {
        const methods = txClient({
            signer: client.signer,
            addr: client.env.rpcURL,
            prefix: client.env.prefix ?? "cosmos",
        });
        this.tx = methods;
        for (let m in methods) {
            this.tx[m] = methods[m].bind(this.tx);
        }
    }
}
;
const Module = (test) => {
    return {
        module: {
            BlitStorage: new SDKModule(test)
        },
        registry: msgTypes
    };
};
export default Module;
//# sourceMappingURL=module.js.map