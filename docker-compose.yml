version: "3.9"

services: 
  blit:
    build: 
      context: .
    image: blitchain/blitchain:${TAG-dev}
    environment:
      - TAG
    ports: 
      - "26656-26657:26656-26657"
      - "1317:1317"
    volumes:
      - ${DEAMON_HOME-~/.blit}:/home/user/.blit
